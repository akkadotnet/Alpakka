# Akka.Streams.Azure.EventHub Simple Producer and Consumer Example

This folder contains an example of how a simple akka stream publishes messages to azure event hubs and another simple akka stream consumes them.

To run the sample, open the Alpakka.slnx solution in your IDE and run the `EventHub.AppHost: https` Aspire launch profile.